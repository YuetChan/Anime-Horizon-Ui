import { PiTimeAgoPipe } from './pi-time-ago.pipe';

describe('PiTimeAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new PiTimeAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
