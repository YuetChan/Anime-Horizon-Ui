import { RandomTextTruncatePipe } from './random-text-truncate.pipe';

describe('RandomTextTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new RandomTextTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
